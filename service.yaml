apiVersion: v1
kind: Service
metadata:
  name: mon-app-js-service
spec:
  type: NodePort
  selector:
    app: mon-app-js
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080
